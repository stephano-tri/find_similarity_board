elastic:
  host: localhost
  port: 9200

spring:
  elasticsearch:
    uris: localhost:9200

logging:
  level:
    com:
      example: debug
    sql: trace
    org:
      springframework:
        data:
          elasticsearch:
            core:
              ReactiveElasticsearchTemplate: trace
              ReactiveElasticsearchClient: trace
              ReactiveElasticsearchRestTemplate: trace

server:
  port: 4383
